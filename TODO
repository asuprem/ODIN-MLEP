> if self.MLEPPipelines[_pipeline]["sequence"] <-- Needs to be changed to extract encoder name from a DataModel class
> Add DAG for pipeline sequence
> Add BatchedLocalWriter
> Add BatchedLocalLoader
> Add BatchedLocalClassLoader

> Add MODEL_PRUNE -- prune old models from database (and memory)
> Add synthetic datasets
> add ARF

> add single-model policy -- only update piplines without generating any new...
> plus validation -- select MUST be recent-updates, etc...